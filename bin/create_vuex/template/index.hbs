import Vue from 'vue'
import Vuex from 'vuex'

import global from './modules/global'
{{#modules}}
  import {{this.name}} from './modules/{{this.name}}'
{{/modules}}

Vue.use(Vuex)

export default new Vuex.Store({
  modules: {
    global,
    {{#modules}}{{this.name}},{{/modules}}
  }
})