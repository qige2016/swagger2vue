import Vue from 'vue'
import Vuex from 'vuex'

import global from './modules/global'
{{#modules}}
  import {{initialLowerCase this.name}} from './modules/{{initialLowerCase this.name}}'
{{/modules}}

Vue.use(Vuex)

export default new Vuex.Store({
  modules: {
    global,
    {{#modules}}{{initialLowerCase this.name}},{{/modules}}
  }
})